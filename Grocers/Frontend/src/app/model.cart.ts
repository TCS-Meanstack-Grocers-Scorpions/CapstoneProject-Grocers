// tslint:disable-next-line:class-name
export class cartProduct {
    constructor(public userId: number, public pid: string, public name: string,
                public price: number, public quantity: number, public imgId: number){

    }
}
